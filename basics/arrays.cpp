#include<bits/stdc++.h>
using namespace std;

int main()
{
    //when you have similar data type
    //this entire array is stored in a memory location
    //1st will be stored after 0th and continues
    //oth can be anywhere in the memory

    int a[4];
    cin>>a[0]>>a[1]>>a[2]>>a[3];

    cout<<a[2];
    return 0;
}